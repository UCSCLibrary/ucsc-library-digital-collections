<%# TODO NEXT what exactly should go in these slots and how do I format it from what I have?%>

<% data = {'autocomplete-url' => property['vocabularies'].first,
           'autocomplete' => key,
           'authority-select' => property['vocabularies']} %>
<% options[:as] = property['input'].to_s %>
<% options[:input_html] = {class: "form-control #{key}-input",data: data} %>
<% options[:wrapper_html] = {data: data} %>

<%= f.input key, options %>
