<ol id="featured-collections">
    <% collections.each do |collection| %>

        <%# REMOVE THE FOLLOWING LINE ONCE FEATURED COLLECTION THINGY WORKS%>
        <% next unless collection.id == 'rx913p88g' %>

        <li class="featured-collection" data-id="<%= collection.id %>">
            <div class="main row">
                <div class="col-sm-3">
                    <%= render_thumbnail_tag collection, { width: 90 } %>
                </div>
                <div class="col-sm-9">
                    <%= render 'hyrax/homepage/featured_fields', featured: collection %>
    </div>
  </div>
</li>
    <% end %>
  <li>
    <%= link_to t('hyrax.homepage.admin_sets.link'),
                main_app.search_catalog_path(f: { human_readable_type_sim: ["Collection"]}),
                class: 'btn btn-default' %>
  </li>
</ol>
