<ol id="featured_collections">
    <% collections.each do |collection| %>

        <%# REMOVE THE FOLLOWING LINE ONCE FEATURED COLLECTION THINGY WORKS%>
        <% next unless ['rx913p88g','2n49t173w'].include? collection.id %>

        <li class="featured-item" data-id="<%= collection.id %>">
            <div class="main row">
                <div class="col-sm-3">
                    <%= render_thumbnail_tag collection, { width: 200 } %>
                </div>
                <div class="col-sm-9">
                    <%= render 'hyrax/homepage/featured_fields', featured: collection %>
    </div>
  </div>
</li>
    <% end %>
</ol>
