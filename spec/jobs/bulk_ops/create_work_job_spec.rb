require 'rails_helper'

RSpec.describe BulkOps::CreateWorkJob do
  describe "a job to create a work" do
    let(:job) {}
    
    context "with valid metadata" do

      it "creates a new work when performed" do
        
      end

      it "updates the status of its work proxy after being performed" do
        
      end

      it "updates the status of operation upon completion if it is the last job in that operation" do
        
      end

      it "triggers the resolution of any relationships involving this work" do
        
      end
    end

    context "with invalid metadata" do
      
    end

  end
end
